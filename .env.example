# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: Override default model
VITE_DEFAULT_MODEL=gpt-4o-mini

# Note: Authentication is now handled by Supabase Auth
# Users will be able to sign up/sign in with email and password
# Their chat sessions will be stored in the Supabase database with RLS enabled
